import React from 'react';
import { ScrollRevealTimeline } from '../ui/ScrollRevealTimeline';

export function NetworkIntroGraph() {
  return <ScrollRevealTimeline />;
}

export default NetworkIntroGraph;
